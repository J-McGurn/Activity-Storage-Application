CREATE TABLE movies (
    movieID INT,
    title VARCHAR(255) PRIMARY KEY,
    rating VARCHAR(45),
    genres VARCHAR(255)
);

CREATE TABLE tvshows (
    showID INT,
    title VARCHAR(255) PRIMARY KEY,
    rating VARCHAR(45),
    genres VARCHAR(255)
);